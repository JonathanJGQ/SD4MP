<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alert.sd4mp.dc.ufc.br.alertnotification" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity

            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="BateryChangeReceiver">
            <intent-filter>
                <action android:name="br.ufc.dc.ck122.action.BATTERY_CHANGED"/>
            </intent-filter>
        </receiver>

        <receiver android:name="PowerConnectedReceiver">
            <intent-filter>
                <action android:name="br.ufc.dc.ck122.action.POWER_CONNECTED"/>
            </intent-filter>
        </receiver>

        <receiver android:name=".PowerDisconnectedReceiver">
            <intent-filter>
                <action android:name="br.ufc.dc.ck122.action.POWER_DISCONNECTED">
                </action>
            </intent-filter>
        </receiver>

        <receiver android:name="AirplaneModeReceiver">
            <intent-filter>

                <action android:name="br.ufc.dc.ck122.action.AIRPLANE">
                </action>
            </intent-filter>
        </receiver>

    </application>

</manifest>
